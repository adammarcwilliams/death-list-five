
const initialState = {
  targets: [],
  flashMessage: null
}

const targets = (state = initialState, action) => {
  switch (action) {
    case 'ACTION_TYPE':
      
      break;
  
    default:
      return state;
  }
}

export default targets;